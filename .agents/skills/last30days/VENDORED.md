# Vendored skill: last30days

Source: `mvanhorn/last30days-skill`

Installed locally so agents working in this repository can discover and use the skill without relying on a per-user global install under `~/.agents/skills`.

To refresh this vendored copy, reinstall from the upstream repository and copy the resulting `last30days` skill directory into `.agents/skills/last30days`.
